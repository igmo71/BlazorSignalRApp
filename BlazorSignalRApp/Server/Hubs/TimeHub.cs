using Microsoft.AspNetCore.SignalR;

namespace BlazorSignalRApp.Server.Hubs
{
    public class TimeHub : Hub
    {
        //public async Task SendTime(DateTime dateTime)
        //{
        //    await Clients.All.SendAsync("ShowTime", dateTime);
        //}
    }
}
